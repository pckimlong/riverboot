name: riverboot
description: "Easy bootstrapping app state with Riverpod"
version: 0.0.1

environment:
  sdk: ^3.9.0-333.2.beta
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
